apiVersion: v1
kind: Service
metadata:
  name: {{ include "image-registry.fullname" . }}
  labels:
    {{- include "image-registry.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "image-registry.selectorLabels" . | nindent 4 }}
